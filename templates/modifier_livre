<!-- templates/modifier_livre.html -->
<html>
    <head>
        <title>Modifier le Livre</title>
    </head>
    <body>
        <h1>Modifier le livre</h1>
        <form method="POST" action="{{ url_for('modifier_livre', id=livre[0]) }}">
            <label for="titre">Titre</label>
            <input type="text" id="titre" name="titre" value="{{ livre[1] }}" required><br><br>

            <label for="auteur">Auteur</label>
            <input type="text" id="auteur" name="auteur" value="{{ livre[2] }}" required><br><br>

            <label for="quantite">Quantité</label>
            <input type="number" id="quantite" name="quantite" value="{{ livre[3] }}" required><br><br>

            <button type="submit">Mettre à jour</button>
        </form>
    </body>
</html>
